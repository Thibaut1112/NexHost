# NexaHost — Git Commit History

This file documents the project's commit history for reference.
To initialize this project as a real Git repository:

```bash
git init
git add .
git commit -m "chore: initial commit"
git branch -M main
git remote add origin https://github.com/yourname/nexahost.git
git push -u origin main
```

---

## Suggested commit messages (chronological)

```
feat: initial project structure and base layout
feat(hero): animated server card widget with live metrics
feat(nav): dark/light mode toggle with localStorage persistence
feat(offers): VPS pricing cards with hover animations
feat(faq): accessible accordion with aria-expanded
feat(contact): form validation and success feedback
feat(scroll): IntersectionObserver reveal animations
fix(mobile): burger menu z-index and overlay positioning
feat(pro): enterprise plans section and feature grid
feat(offers): game server and S3 storage tab panels
feat(comparator): dynamic offer comparison table (max 3)
feat(calculator): real-time price estimator with sliders
feat(map): interactive SVG Europe datacenter map with tooltips
feat(loader): animated page loader with NexaHost branding
feat(timeline): how-it-works 3-step section with stagger
feat(order): 3-step order tunnel with plan selector and payment form
feat(panel): client dashboard with server metrics and invoice list
feat(blog): article grid with 9 hosting-related posts
feat(ping): datacenter ping checker with IP detection
feat(404): custom error page with animated terminal
feat(seo): Open Graph, Twitter Card and canonical meta tags
feat(seo): sitemap.xml and robots.txt
feat(a11y): aria-labels and roles on all interactive elements
chore: add README, CHANGELOG, LICENSE and .gitignore
refactor(js): JSDoc comments and section headers
refactor(css): structured sections with descriptive block headers
chore: bump version to 1.2.0
```
